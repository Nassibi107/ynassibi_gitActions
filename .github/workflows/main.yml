name : First workFlow

on :
  push  :
    branches: 
      - main
jobs:
   example-job:
     runs-on: ubuntu-latest

     steps:
       - name : Print a welcome message
         run : echo "welcome to our first workflow !"

       - name : Checkout repository
         uses : actions/checkout@v6.0.2
         
       - name : list repsitory files
         run  : ls -la
