



on :
  schedule:
    - cron: '0 0 * * *' # Runs every day at midnight

jobs:
  daily-task:
    runs-on: ubuntu-latest

    steps:
      - name: Print a message
        run: echo "This is a daily scheduled task running at midnight!"
